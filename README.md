# Ahmed-AV — IMPORTANT NOTE
ok so newsflash — dragging on tauri v2 is only enabled when withTauriGlobals (the app API) is off, so this version has a working app API, but no dragging.

It took me untold hours to figure this out. Now that I know this I will be moving to the first Ahmed version that has dragging working, since I can just use a Rust backend for this project. The API is less important than I thought it would be.

i'm gonna blow my head off
